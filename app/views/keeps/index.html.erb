<% content_for :title, "Keeps" %>

<%= render "shared/header" do %>
  Keeps
<% end %>

<%= render "shared/subheader" do %>
  Tweets saved from the chopping block
<% end %>

<%= render "shared/timeline", tweets: @tweets %>
