<article class="my-4 border-t">
  <p class="my-4"><%= sanitize tweet.full_text %></p>
  <div class="flex justify-between">
    <%= date_link_to(tweet) %>
    <small class="text-right">
      <%= delete_countdown(tweet) %> (<%= delete_now_link(tweet) %>)
    </small>
  </div>
</article>
